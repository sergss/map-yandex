# map-yandex

Приложение для массовой геокодировки адресов и отображения их на Яндекс.Карте с пронумерованными маркерами.

## Возможности

- 🗺️ Массовая геокодировка списка адресов
- 📍 Автоматическая расстановка пронумерованных маркеров на карте
- ⚙️ Настройка API ключа через интерфейс (сохраняется в localStorage)
- 🔄 Автоматические повторы при ошибках с настраиваемыми задержками
- 📊 Детальная статистика обработки (успешно/ошибки)
- 🎨 Цветной прогресс-бар (зелёный для успеха, красный для ошибок)
- ⏸️ Возможность остановить поиск в любой момент

## Запуск локально

**Требования:** Node.js

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите приложение:
   ```bash
   npm run dev
   ```

3. Откройте браузер и перейдите на [http://localhost:3000](http://localhost:3000)

4. При первом запуске откроется диалог настроек. Введите ваш API ключ Яндекс.Карт:
   - Получите ключ в [Кабинете разработчика](https://developer.tech.yandex.ru/)
   - Настройте количество повторов и задержки при ошибках (опционально)
   - Нажмите "Сохранить"

## Использование

1. Введите список адресов в текстовое поле (каждый адрес с новой строки)
2. Нажмите кнопку "Поиск"
3. Приложение найдёт адреса и разместит маркеры на карте
4. Адреса, которые не удалось найти, будут отображены в списке ошибок

## Настройки

Откройте настройки через кнопку ⚙️ в левой панели:

- **API ключ Яндекс.Карт** - обязательный параметр для работы приложения
- **Количество попыток повтора** - сколько раз повторять запрос при ошибке (по умолчанию 3)
- **Задержки между повторами** - интервалы в секундах между попытками (по умолчанию 1, 2, 4)

Все настройки сохраняются в localStorage браузера.

## Сборка для production

```bash
npm run build
```

Собранные файлы будут в директории `dist/`.
